#include "hzpch.h"
#include "Model.h"

namespace Horizon
{
	void Model::LoadModel(std::string& path)
	{
	}
	
	void Model::ProcessNode(aiNode* node, const aiScene* scene)
	{
	}
	
	Mesh Model::ProcessNode(aiMesh* mesh, const aiScene* scene)
	{
		return Mesh();
	}
}
